const numero = '596'
const numeroConvertido = parseInt(numero)

console.log( typeof numero);
console.log(typeof numeroConvertido);

// Respuesta

//A. String / Number Respuesta Correcta
//B. String / String
//B. Number / String